import Link from "next/link";import {CustomerJourney} from "@/components/customer/customer-journey";import {SiteFooter} from "@/components/layout/site-footer";import {SiteHeader} from "@/components/layout/site-header";
export default async function Page({params}:{params:Promise<{slug:string;reservationId:string}>}){const {slug,reservationId}=await params;return <main className="min-h-screen bg-background"><SiteHeader/><div className="container-page py-5 text-sm text-ink-500"><Link href={`/expedicoes/${slug}` as never}>Expedição</Link><span className="px-2">›</span>Minha Expedição</div><CustomerJourney reservationId={reservationId}/><SiteFooter/></main>}
